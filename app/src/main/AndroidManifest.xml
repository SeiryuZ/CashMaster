<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.halim.adam.cashmaster">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddNewSpending">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name=".AddNewSpending" />-->
        <activity android:name=".AddNewIncome" />
        <activity android:name=".AddNewBudget" />
        <activity android:name=".TransactionHistory" />
        <activity android:name=".Statistics"></activity>
        <activity android:name=".AddNewCategory"></activity>
    </application>

</manifest>